/*

WHAT WE DO IN FIXED SIZE SLIDING WINDOW 

1. first we find the prevsum which is the sum of the first window 
2. then initialize -> maxsum , maxidx
3. then initialize -> left and right of the next Windows
4. while(r < n) -> create {currsum} , update {maxsum , maxidx , prevsum}

*/